{  
    "servers":{
        "main": {
            "servername": "Main server",
            "serverfile": "server.jar",
            "minMemory" : 1,
            "maxMemory" : 1,
            "serverdir": "/home/server/",
            "rconconfig": ["127.0.0.1", 25567, "password"],
            "isNoGUI" : true
        }
    },
    "botconfig":{
        "_comment": "token is botttoken:string, botadmin is botadmin id:integer, prefix is bot prefix:string",
        "token": "bottoken",
        "botadmin": null,
        "prefix": ";"
    }
}